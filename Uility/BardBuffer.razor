if findbuff 'Provocation'
else
    overhead 'MISSING PROVO BUFFS'
endif
if findbuff 'Peacemaking'
else
    overhead 'MISSING PEACE BUFFS'
endif
if findbuff 'Discordance'
else
    overhead 'MISSING DISCO BUFFS'
endif
if findbuff 'Strength'
else
    overhead 'Missing str pot'
endif
if findbuff 'Agility'
else
    overhead 'Missing agility pot'
endif
if findbuff 'Magic resist potion'
else
    overhead 'Missing MR pot'
endif
if lhandempty 
    overhead 'EQUIP YOUR SHIELD'
endif
   